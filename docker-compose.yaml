services:
  nginx:
    image: tiangolo/nginx-rtmp:latest
    ports:
      - 80:80
      - 1935:1935
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./html:/var/www/html
  php:
    image: php:8.3-fpm
    volumes:
      - ./php:/var/www/html
      - ./data:/var/www/data